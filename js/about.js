/* ==========================================================================
   STITBD — About Page Script
   Only about.html-specific logic goes here. All shared/common logic
   (navbar, footer, dropdowns, quote modal, company info, animations) lives
   in js/app.js and is loaded before this file on about.html — never
   duplicate that logic here.
   ========================================================================== */

// No about-page-only behavior needed right now — js/app.js already handles
// everything (navbar, footer, dropdowns, quote modal, animations, AOS init)
// since about.html reuses the same element IDs/markup.
// Add new code here ONLY when a feature is unique to about.html.